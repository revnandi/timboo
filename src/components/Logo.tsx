import React from 'react';
import classes from '../scss/Logo.module.scss';

function Logo() {
  return <div className={ classes.Container }>
      <h1 className={ classes.Text }>Timboo</h1>
    </div>
}

export default Logo;