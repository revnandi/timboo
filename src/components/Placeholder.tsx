import React from 'react';
import classes from '../scss/Placeholder.module.scss';

function Placeholder() {
  return <div className={ classes.Container }>
    <div className={ classes.Inner }>Timboo</div>
    </div>
}

export default Placeholder;